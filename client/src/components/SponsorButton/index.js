import React from 'react';
import "./style.css";

function SponsorButton({ children }) {
    return <button type="button" class="btn btn-primary btn-sponsor">Sponsor Me!</button>
};

export default SponsorButton;
